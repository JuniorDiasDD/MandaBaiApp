class Employee {
  String name, cargo, image, description,tel,email;
  Employee(
      {required this.name,
      required this.cargo,
      required this.image,
       required this.tel,
        required this.email,
      required this.description});
}
